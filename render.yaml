services:
  - type: web
    name: 2careai-backend
    runtime: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.app.server:create_app --host 0.0.0.0 --port $PORT --factory
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
